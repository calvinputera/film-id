import React from "react";

const SecodaryButton = ({ title }) => {
	return (
		<div className="bg-white text-primaryBlack py-3 px-5 w-36 rounded-lg text-center font-semibold">
			{title}
		</div>
	);
};

export default SecodaryButton;
